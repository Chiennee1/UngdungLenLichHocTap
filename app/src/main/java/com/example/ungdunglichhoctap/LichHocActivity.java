package com.example.ungdunglichhoctap;

import android.os.Bundle;

import androidx.appcompat.app.AppCompatActivity;

public class LichHocActivity extends AppCompatActivity {
    // This activity can be used to display the schedule of classes or lessons.
    // You can add UI elements in the corresponding XML layout file (activity_lich_hoc.xml) and implement functionality here.

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_lichhoc);

        // Initialize UI elements and set up click listeners if needed
    }
}
